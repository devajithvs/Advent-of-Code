def Solution(file_content):
    N = len(file_content)
    M = len(file_content[0].strip())
    A = [int(x.strip()) for x in file_content[:]] # O(N) time complexity
    total = sum(A) # O(N) time complexity

    


    